node_modules
dist
*.local
.env
.env.*
.DS_Store

# Cursor agent debug logs (local only)
.cursor/debug*.log

# Supabase CLI cache at repo root (project lives under ai-writing-app/supabase)
supabase/.temp/
